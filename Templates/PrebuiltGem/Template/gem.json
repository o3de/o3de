{
    "gem_name": "${Name}",
    "version": "${Version}",
    "display_name": "${DisplayName}",
    "license": "${License}",
    "license_url": "${LicenseURL}",
    "origin": "${Origin}",
    "origin_url": "${OriginURL}",
    "type": "Code",
    "summary": "A short description for Gem ${Name} which provides pre-built binaries and API interfaces goes here.",
    "canonical_tags": [
        "Gem"
    ],
    "user_tags": [
        "${UserTags}"
    ],
    "platforms": [
        "${Platforms}"
    ],
    "icon_path": "${IconPath}",
    "requirements": "${Requirements}",
    "documentation_url": "${DocumentationURL}",
    "dependencies": [
    ],
    "repo_uri": "${RepoURI}",
    "compatible_engines": [
    ],
    "engine_api_dependencies":[
    ]
}
